import { createContentPage } from "@/components/store/ContentPage";

export const metadata = { title: "Support" };

export default createContentPage(
  "support.md",
  "Support",
  "Get help with purchases and package delivery."
);
